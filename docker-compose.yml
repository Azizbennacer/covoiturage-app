version: "3.9"

services:
  backend:
    build: ./backend
    container_name: covoiturage-back
    ports:
      - "8080:8080"

  frontend:
    build: ./Frontend
    container_name: covoiturage-front
    ports:
      - "4200:80"
    depends_on:
      - backend
